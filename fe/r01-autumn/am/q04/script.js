"use script";

const a = 2;
const b = 3;

const f = (t) => {
  f(t) = a / (t + 1);
};

const g = (t) => {
  g(t) = b / (t^ - t);
};

lim = g(t) / f(t);
lim = (b / (t^ - t)) / (a / (t + 1));
lim = (b / (t^ - t)) * ((t + 1) / a);
lim = b(t + 1) / a(t^ - t);

t = 100;
b(100 + 1) / a(100 * 100 - 100)
11b / 90a = 0.1222222
101b / 9900a = 0.0102....